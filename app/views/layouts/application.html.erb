<!DOCTYPE html>
<html>
  <head>
    <title>DeliveryApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <nav>
      <%= link_to 'INÍCIO', root_path %>
      <%= link_to 'TRANSPORTADORAS', root_path %>
      <%= link_to 'LOGIN', root_path %>
    </nav>
    <%= yield %>
  </body>
</html>
